In lobby:

In game room:

In game:
    Give the option to select which player to view (during running phase).
    Hold shift to upgrade tower with left click.

General:
    Challenge mode?
    Option available after completed game.
    Display two mazes side by side.
    Fix error: The AudioContext was not allowed to start. It must be resumed (or created) after a user gesture on the page. https://goo.gl/7K7WLu
        On the starting page

Grid appearance changes:
    Fix 'grid lines' appearance in the center.
    Runner appearance change.
    Performance updates:
        - Put everything which is static in it's own layer which is cached.
        - For moving parts (slowTower, runner, selection square, clapAnimation). Keep separate 
            and make sure only the part re-renders
